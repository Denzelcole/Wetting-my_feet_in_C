#include <stdio.h>

int main(void)
{
        /*
         *This is a comment section it's ignored by code
         */

        // This can also be used as a comment too
        printf("Comments are ignored but used as a reference\n");
        return (0);
}